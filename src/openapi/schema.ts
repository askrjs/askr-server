// Kept as an internal import shim. The executable schema implementation lives
// in @askrjs/schema and is deliberately shared by runtime validation and OpenAPI.
export { schema } from "@askrjs/schema";
